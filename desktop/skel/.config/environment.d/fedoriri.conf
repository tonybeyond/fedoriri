# Variables de session (systemd user) — lues par niri-session.
# Les applis Electron (dont Shadow) choisissent Wayland quand possible ;
# Shadow lui-même tourne en X11 via xwayland-satellite, c'est voulu.
ELECTRON_OZONE_PLATFORM_HINT=auto
MOZ_ENABLE_WAYLAND=1
QT_QPA_PLATFORM=wayland
